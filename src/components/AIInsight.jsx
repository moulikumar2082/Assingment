function AIInsight({ analysis }) {
  const detected = analysis?.skills?.filter((skill) => !['Resume uploaded'].includes(skill)).slice(0, 3).join(', ') || 'Python, SQL and Power BI'
  return <section className="ai-section" id="insight"><div className="ai-inner section-shell"><div className="ai-copy"><p className="kicker light-kicker"><span className="kicker-dot" /> THE NEXT SIGNAL</p><h2>The map is useful when it tells you <i>what to do next.</i></h2><p>CareerLens AI turns your profile into a focused recommendation, not a wall of generic advice.</p><a className="light-link" href="#career-map">Explore the paths <span aria-hidden="true">↗</span></a></div><div className="recommendation"><div className="recommendation-head"><span className="ai-spark">✦</span><span>CareerLens AI</span><span className="recommendation-status">{analysis ? 'RESUME INSIGHT' : 'DEMO INSIGHT'}</span></div><div className="recommendation-body"><p>Your profile shows strong signals in <strong>{detected}.</strong></p><span className="recommendation-label">YOUR NEXT OPPORTUNITY</span><h3>Machine Learning</h3><span className="recommendation-label">RECOMMENDED NEXT STEPS</span><ol><li><b>01</b>Build an applied ML project</li><li><b>02</b>Strengthen model evaluation</li><li><b>03</b>Learn model deployment</li></ol><div className="why"><span>WHY?</span><p>Deeper modeling skills could open additional Data Scientist opportunities.</p></div></div></div></div></section>
}

export default AIInsight
